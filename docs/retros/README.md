# Staging retrospectives

Immature or one-off session learnings land here as:

```text
YYYY-MM-DD-short-slug.md
```

Promote recurring items into `skills/` or `rules/` via the **Retrospective** skill (with approval). Prune or archive when promoted.
